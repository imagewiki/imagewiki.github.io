.row.quick-menu
  .col-lg-8.col-md-8.col-sm-8.col-xs-12.col-lg-offset-2.col-md-offset-2.col-sm-offset-2.content-main
    ul.breadcrumb
      li
        a(ui-sref='home') Home
      li Image Ownership

    .alert.alert-danger(ng-if="isTempImage") You must <strong>create and account</strong> or <strong>log in</strong> in order to save this image

    form#info_image.edit_image(name="imageForm", ng-submit="updateImage(image)")
      .image-info.clearfix
        .image
          img(alt='', ng-src='{{image.reference_file_path}}')
        .title
          editable-field(type="'text'", name="'title'", ng-model="image.title", block="'h3'")
        .creator(ng-show="image.creator.creator_name != ''")
          | {{ image.creator.creator_name }}
      .page-header
        //- Include ImageViolation button partial
        image-violation(image-id="image.image_id")

        h2(ng-click="debugImage()") Copyright and ownership Information
      .image-fields(ng-include="'app/images/images-fields.html'", onload="checkEditing()")
